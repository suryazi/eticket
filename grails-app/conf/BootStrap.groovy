class BootStrap {

    def init = { servletContext ->
        /*if(!User.findByUserId("suryazi")){
            User.fin
            def user = new User()
            user.userId='suryazi'
            user.password='kadapa516001'.encodeAsSHA256()
            user.level='Admin'
            user.dateCreated=new Date()
            user.lastUpdate=new Date()
            user.save(flush:true)
        }*/
    }
    def destroy = {
    }
}
